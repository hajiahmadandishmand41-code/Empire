import { prisma, isDatabaseConfigured } from '@/lib/db';
import { mockStats, mockProducts, mockCategories, mockOrders, mockUsers, mockSalesByDay, mockTopProducts, mockSellers, mockTransactions, mockRevenueSummary, type AdminStats, type AdminOrderRow, type AdminUserRow, type AdminProductRow, type AdminCategoryRow, type SalesByDay, type TopProduct, type AdminSellerRow, type AdminTransactionRow, type AdminRevenueSummary, type SellerStatus } from './mock-data';
const isDev=process.env.NODE_ENV!=='production';
export interface ListFilters{q?:string;page?:number;pageSize?:number} export interface OrderFilters extends ListFilters{status?:string} export interface Paged<T>{items:T[];total:number;page:number;pageSize:number;source:'db'|'mock'}
function paginate<T>(items:T[],page=1,pageSize=10,source:'db'|'mock'='mock'):Paged<T>{const total=items.length;return{items:items.slice((page-1)*pageSize,(page-1)*pageSize+pageSize),total,page,pageSize,source};}
export async function getAdminStats():Promise<AdminStats&{source:'db'|'mock'}>{if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');return{...mockStats,source:'mock'}}try{const[users,products,orders,categories,agg]=await Promise.all([prisma.user.count(),prisma.product.count(),prisma.order.count(),prisma.category.count(),prisma.order.aggregate({_sum:{total:true}})]);if(isDev&&users===0&&products===0&&orders===0&&categories===0)return{...mockStats,source:'mock'};return{users,products,orders,categories,revenue:Number(agg._sum.total??0),currency:'AFN',source:'db'};}catch(err){console.error('[admin/stats] DB error:',err);if(isDev)return{...mockStats,source:'mock'};throw err;}}
export async function listAdminProducts(f:ListFilters={}):Promise<Paged<AdminProductRow>>{const page=f.page??1,pageSize=f.pageSize??10,q=f.q?.trim().toLowerCase()??'';if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');return paginate(q?mockProducts.filter(p=>p.name.toLowerCase().includes(q)):mockProducts,page,pageSize,'mock')}try{const where=q?{OR:[{name:{contains:q,mode:'insensitive' as const}},{slug:{contains:q,mode:'insensitive' as const}},{region:{contains:q,mode:'insensitive' as const}}]}:{};const[rows,total]=await Promise.all([prisma.product.findMany({where,include:{category:true},orderBy:{createdAt:'desc'},take:pageSize,skip:(page-1)*pageSize}),prisma.product.count({where})]);if(isDev&&total===0&&!q)return paginate(mockProducts,page,pageSize,'mock');return{items:rows.map(p=>({id:p.id,slug:p.slug,name:p.name,price:Number(p.price),currency:p.currency,categoryName:p.category.name,region:p.region,inStock:p.inStock,createdAt:p.createdAt.toISOString()})),total,page,pageSize,source:'db'};}catch(err){console.error('[admin/products] DB error:',err);if(isDev)return paginate(mockProducts,page,pageSize,'mock');throw err;}}
export async function listAdminCategories():Promise<{items:AdminCategoryRow[];source:'db'|'mock'}>{if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');return{items:mockCategories,source:'mock'}}try{const rows=await prisma.category.findMany({include:{_count:{select:{products:true}}},orderBy:{name:'asc'}});if(isDev&&!rows.length)return{items:mockCategories,source:'mock'};return{items:rows.map(c=>({id:c.id,key:c.key,name:c.name,slug:c.slug,productCount:c._count?.products??0})),source:'db'};}catch(err){console.error('[admin/categories] DB error:',err);if(isDev)return{items:mockCategories,source:'mock'};throw err;}}
export async function listAdminOrders(f:OrderFilters={}):Promise<Paged<AdminOrderRow>>{const page=f.page??1,pageSize=f.pageSize??10,q=f.q?.trim()??'';if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');let items=mockOrders;if(f.status)items=items.filter(o=>o.status===f.status);if(q)items=items.filter(o=>o.reference.includes(q)||o.customerName.toLowerCase().includes(q.toLowerCase()));return paginate(items,page,pageSize,'mock')}try{const where={...(f.status?{status:f.status as never}:{}),...(q?{OR:[{reference:{contains:q,mode:'insensitive' as const}},{address:{fullName:{contains:q,mode:'insensitive' as const}}}]}:{})};const[rows,total]=await Promise.all([prisma.order.findMany({where,include:{address:true},orderBy:{createdAt:'desc'},take:pageSize,skip:(page-1)*pageSize}),prisma.order.count({where})]);if(isDev&&total===0&&!q&&!f.status)return paginate(mockOrders,page,pageSize,'mock');return{items:rows.map(o=>({id:o.id,reference:o.reference,status:o.status,paymentMethod:o.paymentMethod,total:Number(o.total),currency:o.currency,itemCount:o.itemCount,customerName:o.address.fullName,createdAt:o.createdAt.toISOString()})),total,page,pageSize,source:'db'};}catch(err){console.error('[admin/orders] DB error:',err);if(isDev)return paginate(mockOrders,page,pageSize,'mock');throw err;}}
export async function listAdminUsers(f:ListFilters={}):Promise<Paged<AdminUserRow>>{const page=f.page??1,pageSize=f.pageSize??10,q=f.q?.trim().toLowerCase()??'';if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');return paginate(q?mockUsers.filter(u=>u.fullName.toLowerCase().includes(q)||(u.email??'').toLowerCase().includes(q)||(u.phone??'').includes(q)):mockUsers,page,pageSize,'mock')}try{const where=q?{OR:[{fullName:{contains:q,mode:'insensitive' as const}},{email:{contains:q,mode:'insensitive' as const}},{phone:{contains:q}}]}:{};const[rows,total]=await Promise.all([prisma.user.findMany({where,orderBy:{createdAt:'desc'},take:pageSize,skip:(page-1)*pageSize,include:{_count:{select:{orders:true}}}}),prisma.user.count({where})]);if(isDev&&total===0&&!q)return paginate(mockUsers,page,pageSize,'mock');return{items:rows.map(u=>{const raw=u as unknown as{role?:string;isActive?:boolean;_count?:{orders:number}};const role:AdminUserRow['role']=raw.role==='admin'||raw.role==='seller'?raw.role:'customer';return{id:u.id,fullName:u.fullName,email:u.email,phone:u.phone,role,isActive:raw.isActive??true,createdAt:u.createdAt.toISOString(),orderCount:raw._count?.orders??0}}),total,page,pageSize,source:'db'};}catch(err){console.error('[admin/users] DB error:',err);if(isDev)return paginate(mockUsers,page,pageSize,'mock');throw err;}}
export async function getSalesByDay(days=14):Promise<{items:SalesByDay[];source:'db'|'mock'}>{if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');return{items:mockSalesByDay(days),source:'mock'}}try{const since=new Date(Date.now()-days*86400000),rows=await prisma.order.findMany({where:{createdAt:{gte:since}},select:{createdAt:true,total:true}});if(isDev&&!rows.length)return{items:mockSalesByDay(days),source:'mock'};const buckets=new Map<string,{count:number;revenue:number}>();for(let i=0;i<days;i++){const d=new Date(Date.now()-(days-1-i)*86400000).toISOString().slice(0,10);buckets.set(d,{count:0,revenue:0})}for(const r of rows){const b=buckets.get(r.createdAt.toISOString().slice(0,10));if(b){b.count++;b.revenue+=Number(r.total)}}return{items:Array.from(buckets.entries()).map(([date,v])=>({date,...v})),source:'db'};}catch(err){console.error('[admin/sales-by-day] DB error:',err);if(isDev)return{items:mockSalesByDay(days),source:'mock'};throw err;}}
export async function getTopProducts(limit=5):Promise<{items:TopProduct[];source:'db'|'mock'}>{if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');return{items:mockTopProducts(limit),source:'mock'}}try{const grouped=await prisma.orderItem.groupBy({by:['slug','name'],_sum:{quantity:true,price:true},orderBy:{_sum:{quantity:'desc'}},take:limit});if(isDev&&!grouped.length)return{items:mockTopProducts(limit),source:'mock'};return{items:grouped.map(g=>({slug:g.slug,name:g.name,units:g._sum.quantity??0,revenue:Number(g._sum.price??0)})),source:'db'};}catch(err){console.error('[admin/top-products] DB error:',err);if(isDev)return{items:mockTopProducts(limit),source:'mock'};throw err;}}
export interface SellerFilters extends ListFilters{status?:SellerStatus|'all'} export interface TransactionFilters extends ListFilters{status?:string;method?:string}
export async function listAdminSellers(f:SellerFilters={}):Promise<Paged<AdminSellerRow>>{const page=f.page??1,pageSize=f.pageSize??10,q=f.q?.trim().toLowerCase()??'',status=f.status&&f.status!=='all'?f.status:undefined;if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');let items=mockSellers;if(status)items=items.filter(s=>s.sellerStatus===status);if(q)items=items.filter(s=>s.fullName.toLowerCase().includes(q)||(s.email??'').toLowerCase().includes(q)||(s.shopName??'').toLowerCase().includes(q));return paginate(items,page,pageSize,'mock')}try{const where:Record<string,unknown>={OR:[{role:'seller' as never},{sellerStatus:{not:'none'} as never}]};if(status)where.sellerStatus=status as never;if(q)where.AND=[{OR:[{fullName:{contains:q,mode:'insensitive' as const}},{email:{contains:q,mode:'insensitive' as const}},{sellerShopName:{contains:q,mode:'insensitive' as const}}]}];const[rows,total]=await Promise.all([prisma.user.findMany({where:where as never,orderBy:{createdAt:'desc'},take:pageSize,skip:(page-1)*pageSize,include:{_count:{select:{products:true}}}}),prisma.user.count({where:where as never})]);if(isDev&&total===0&&!q&&!status)return paginate(mockSellers,page,pageSize,'mock');return{items:rows.map(u=>{const raw=u as unknown as{sellerStatus?:SellerStatus;sellerShopName?:string|null;sellerBio?:string|null;isActive?:boolean;_count?:{products?:number}};return{id:u.id,fullName:u.fullName,email:u.email,phone:u.phone,shopName:raw.sellerShopName??null,bio:raw.sellerBio??null,sellerStatus:raw.sellerStatus??'none',isActive:raw.isActive??true,productCount:raw._count?.products??0,createdAt:u.createdAt.toISOString()}}),total,page,pageSize,source:'db'};}catch(err){console.error('[admin/sellers] DB error:',err);if(isDev)return paginate(mockSellers,page,pageSize,'mock');throw err;}}
export async function getAdminSeller(id:string):Promise<AdminSellerRow|null>{if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');return mockSellers.find(s=>s.id===id)??null}try{const u=await prisma.user.findUnique({where:{id},include:{_count:{select:{products:true}}}});if(!u)return null;const raw=u as unknown as{sellerStatus?:SellerStatus;sellerShopName?:string|null;sellerBio?:string|null;isActive?:boolean;_count?:{products?:number}};return{id:u.id,fullName:u.fullName,email:u.email,phone:u.phone,shopName:raw.sellerShopName??null,bio:raw.sellerBio??null,sellerStatus:raw.sellerStatus??'none',isActive:raw.isActive??true,productCount:raw._count?.products??0,createdAt:u.createdAt.toISOString()};}catch(err){console.error('[admin/seller] DB error:',err);if(isDev)return mockSellers.find(s=>s.id===id)??null;throw err;}}
export async function listAdminTransactions(f:TransactionFilters={}):Promise<Paged<AdminTransactionRow>>{const page=f.page??1,pageSize=f.pageSize??10,q=f.q?.trim()??'';if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');let items=mockTransactions;if(f.status)items=items.filter(t=>t.status===f.status);if(f.method)items=items.filter(t=>t.method===f.method);if(q)items=items.filter(t=>t.reference.toLowerCase().includes(q.toLowerCase())||t.orderReference.toLowerCase().includes(q.toLowerCase()));return paginate(items,page,pageSize,'mock')}try{const where:Record<string,unknown>={};if(f.status)where.status=f.status as never;if(f.method)where.method=f.method as never;if(q)where.OR=[{reference:{contains:q,mode:'insensitive' as const}},{order:{reference:{contains:q,mode:'insensitive' as const}}}];const[rows,total]=await Promise.all([prisma.transaction.findMany({where:where as never,include:{order:{select:{reference:true}}},orderBy:{createdAt:'desc'},take:pageSize,skip:(page-1)*pageSize}),prisma.transaction.count({where:where as never})]);if(isDev&&total===0&&!q&&!f.status&&!f.method)return paginate(mockTransactions,page,pageSize,'mock');return{items:rows.map(t=>({id:t.id,reference:t.reference,orderId:t.orderId,orderReference:t.order.reference,provider:t.provider,method:t.method,status:t.status,amount:Number(t.amount),currency:t.currency,paidAt:t.paidAt?t.paidAt.toISOString():null,createdAt:t.createdAt.toISOString()})),total,page,pageSize,source:'db'};}catch(err){console.error('[admin/transactions] DB error:',err);if(isDev)return paginate(mockTransactions,page,pageSize,'mock');throw err;}}
export async function getAdminRevenue(days=30):Promise<AdminRevenueSummary&{source:'db'|'mock'}>{if(!isDatabaseConfigured()){if(!isDev)throw new Error('Database not configured');return{...mockRevenueSummary(days),source:'mock'}}try{const since=new Date(Date.now()-days*86400000);const[orders,txns]=await Promise.all([prisma.order.findMany({where:{createdAt:{gte:since}},select:{createdAt:true,total:true,paymentStatus:true,paymentMethod:true}}),prisma.transaction.findMany({where:{createdAt:{gte:since}},select:{amount:true,status:true,method:true}})]);if(isDev&&!orders.length&&!txns.length)return{...mockRevenueSummary(days),source:'mock'};const gross=orders.reduce((s,o)=>s+Number(o.total),0),paid=orders.filter(o=>o.paymentStatus==='paid').reduce((s,o)=>s+Number(o.total),0),pending=orders.filter(o=>o.paymentStatus==='pending').reduce((s,o)=>s+Number(o.total),0),refunded=txns.filter(t=>t.status==='refunded').reduce((s,t)=>s+Number(t.amount),0),paidOrderCount=orders.filter(o=>o.paymentStatus==='paid').length;const buckets=new Map<string,{count:number;revenue:number}>();for(let i=0;i<days;i++){const d=new Date(Date.now()-(days-1-i)*86400000).toISOString().slice(0,10);buckets.set(d,{count:0,revenue:0})}for(const o of orders){const b=buckets.get(o.createdAt.toISOString().slice(0,10));if(b){b.count++;b.revenue+=Number(o.total)}}const methodMap=new Map<string,{amount:number;count:number}>();for(const t of txns.filter(x=>x.status==='paid')){const m=methodMap.get(t.method)??{amount:0,count:0};m.amount+=Number(t.amount);m.count++;methodMap.set(t.method,m)}return{gross,paid,pending,refunded,currency:'AFN',orderCount:orders.length,paidOrderCount,averageOrderValue:paidOrderCount?Math.round(paid/paidOrderCount):0,byDay:Array.from(buckets.entries()).map(([date,v])=>({date,...v})),byMethod:Array.from(methodMap.entries()).map(([method,v])=>({method,...v})),source:'db'};}catch(err){console.error('[admin/revenue] DB error:',err);if(isDev)return{...mockRevenueSummary(days),source:'mock'};throw err;}}
