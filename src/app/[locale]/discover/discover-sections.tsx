import Image from 'next/image';
import { ArrowLeft, BadgeCheck, MapPin, Package, ShieldCheck, Store } from 'lucide-react';
import { Link } from '@/i18n/routing';
import { ProductSliderSection } from '@/features/home/components/product-slider-section';
import { PersonalizedProductsSection, RecentlyViewedSection } from '@/features/home/components/personalized-products-section';
import { getHomepageDataState, getHomepageSectionState, toSliderProduct } from '@/features/home/lib/homepage-data';
import { getSellerRepository } from '@/server/infrastructure/registry';
import { isDatabaseConfigured } from '@/lib/db';

type Locale = 'fa' | 'ps' | 'en';
const copy = (locale: Locale) => locale === 'en' ? { stores: 'Stores', brands: 'Brands', storesTitle: 'Explore trusted stores', storesSubtitle: 'Meet verified sellers and open their storefronts directly.', viewStores: 'View all stores', verified: 'Verified seller', products: 'products', unavailable: 'The database is not configured for this preview yet.' } : locale === 'ps' ? { stores: 'پلورنځي', brands: 'برانډونه', storesTitle: 'باوري پلورنځي وپلټئ', storesSubtitle: 'تایید شوي پلورونکي ومومئ او د هغوی پلورنځي مستقیم خلاص کړئ.', viewStores: 'ټول پلورنځي وګورئ', verified: 'تایید شوی پلورونکی', products: 'محصولات', unavailable: 'د دې مخکتنې لپاره د ډیټابېس پیوستون لا نه دی برابر شوی.' } : { stores: 'فروشگاه‌ها', brands: 'برندها', storesTitle: 'فروشگاه‌های معتبر را کشف کنید', storesSubtitle: 'فروشندگان تأییدشده را ببینید و مستقیم وارد فروشگاهشان شوید.', viewStores: 'مشاهده همه فروشگاه‌ها', verified: 'فروشنده تأییدشده', products: 'محصول', unavailable: 'اتصال پایگاه داده برای این پیش‌نمایش هنوز تنظیم نشده است.' };

export function DiscoverUnavailable({ locale }: { locale: Locale }) { return <div className="mx-auto my-6 max-w-screen-xl px-3 sm:px-6"><div className="rounded-3xl border border-amber-300/40 bg-amber-50/60 px-6 py-12 text-center text-sm font-semibold text-amber-900 dark:border-amber-500/20 dark:bg-amber-950/20 dark:text-amber-200">{copy(locale).unavailable}</div></div>; }

async function productState(section: 'featured' | 'bestSelling' | 'newest' | 'popular') { return getHomepageSectionState(section, 12); }
export async function DiscoverProductSection({ section, locale, title, subtitle, href, badge, accentColor }: { section: 'featured' | 'bestSelling' | 'newest' | 'popular'; locale: Locale; title: string; subtitle: string; href: string; badge: string; accentColor: string }) { const state = await productState(section); if (state.status === 'unavailable') return <DiscoverUnavailable locale={locale} />; if (!state.products.length) return null; return <ProductSliderSection title={title} subtitle={subtitle} viewAllHref={href} products={state.products.map((product) => toSliderProduct(product, badge))} locale={locale} accentColor={accentColor} />; }

async function storeState() { if (!isDatabaseConfigured()) return { ok: false as const, items: [] }; try { const result = await getSellerRepository().findPublicMany({ q: '', page: 1, pageSize: 4 }); return { ok: true as const, items: result.items }; } catch { return { ok: false as const, items: [] }; } }
export async function TrustedStores({ locale }: { locale: Locale }) { const state = await storeState(); if (!state.ok) return <DiscoverUnavailable locale={locale} />; if (!state.items.length) return null; const t = copy(locale); return <section id="stores" aria-labelledby="stores-title" className="mx-auto max-w-screen-xl px-3 py-6 sm:px-6 sm:py-9"><div className="mb-5 flex items-end justify-between gap-3"><div><div className="inline-flex items-center gap-2 rounded-full bg-violet-500/10 px-3 py-1.5 text-[10px] font-extrabold text-violet-600 dark:text-violet-400"><Store className="h-3.5 w-3.5" aria-hidden />{t.stores}</div><h2 id="stores-title" className="mt-3 text-xl font-black tracking-tight sm:text-2xl">{t.storesTitle}</h2><p className="mt-1 text-xs leading-6 text-muted-foreground sm:text-sm">{t.storesSubtitle}</p></div><div className="flex shrink-0 items-center gap-2"><Link href="/stores" className="inline-flex items-center gap-1.5 rounded-xl border border-border bg-card px-3.5 py-2 text-xs font-bold text-muted-foreground transition hover:border-primary/25 hover:text-primary">{t.viewStores}<ArrowLeft className="h-3.5 w-3.5 rtl:rotate-180" aria-hidden /></Link><Link href="/brands" className="inline-flex items-center gap-1.5 rounded-xl border border-primary/20 bg-primary/5 px-3.5 py-2 text-xs font-bold text-primary transition hover:bg-primary/10"><BadgeCheck className="h-3.5 w-3.5" aria-hidden />{t.brands}</Link></div></div><div className="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-4">{state.items.map((store) => <Link key={store.id} href={`/store/${store.id}` as never} className="group min-w-0 overflow-hidden rounded-2xl border border-border bg-card shadow-sm transition-[transform,box-shadow,border-color] duration-200 hover:-translate-y-0.5 hover:border-primary/25 hover:shadow-md"><div className="relative aspect-[2.8/1] overflow-hidden bg-muted">{store.bannerUrl ? <Image src={store.bannerUrl} alt="" fill sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw" className="object-cover" /> : <div className="h-full w-full bg-gradient-to-br from-violet-500/15 via-muted to-muted" />}</div><div className="p-3"><div className="flex items-center gap-2.5"><div className="relative h-10 w-10 shrink-0 overflow-hidden rounded-xl border border-border bg-background">{store.logoUrl ? <Image src={store.logoUrl} alt={store.shopName} fill sizes="40px" className="object-cover" /> : <div className="flex h-full w-full items-center justify-center bg-primary text-xs font-black text-primary-foreground">{store.shopName.charAt(0)}</div>}</div><div className="min-w-0"><h3 className="truncate text-sm font-black group-hover:text-primary">{store.shopName}</h3><span className="mt-1 inline-flex items-center gap-1 text-[9px] font-bold text-emerald-600 dark:text-emerald-400"><ShieldCheck className="h-3 w-3" aria-hidden />{t.verified}</span></div></div><div className="mt-3 flex gap-2 text-[9px] text-muted-foreground"><span className="inline-flex min-w-0 items-center gap-1 rounded-lg border border-border bg-background px-2 py-1.5"><Package className="h-3 w-3 shrink-0" aria-hidden />{store.productCount} {t.products}</span>{store.city ? <span className="inline-flex min-w-0 items-center gap-1 rounded-lg border border-border bg-background px-2 py-1.5"><MapPin className="h-3 w-3 shrink-0" aria-hidden /><span className="truncate">{store.city}</span></span> : null}</div></div></Link>)}</div></section>; }

async function recommendationState() { const state = await getHomepageDataState(); if (state.status === 'unavailable') return { ok: false as const, pool: [] as Awaited<ReturnType<typeof getHomepageDataState>>['data']['featured'] }; const data = state.data; const seen = new Set<string>(); const pool = [...data.featured, ...data.bestSelling, ...data.newest, ...data.popular].filter((product) => !seen.has(product.id) && seen.add(product.id)).slice(0, 12); return { ok: true as const, pool }; }
export async function Recommendations({ locale }: { locale: Locale }) { const state = await recommendationState(); if (!state.ok) return <DiscoverUnavailable locale={locale} />; if (!state.pool.length) return null; const products = state.pool.map((product) => toSliderProduct(product)); return <><PersonalizedProductsSection products={products} locale={locale} /><RecentlyViewedSection products={products} locale={locale} /></>; }
