import { getAdminDashboardMetrics } from '@/features/admin/lib/dashboard';
import { formatMoney } from '@/features/admin/lib/format';
export const dynamic='force-dynamic';
export default async function AnalyticsPage(){const d=await getAdminDashboardMetrics();return <div className="space-y-6"><header><h1 className="text-2xl font-black">Analytics Center</h1><p className="mt-1 text-sm text-muted-foreground">Revenue، Orders، AOV، Top Products، Categories و Sellers از Database واقعی.</p></header><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div className="rounded-2xl border border-border bg-card p-5"><div className="text-xs text-muted-foreground">Revenue</div><div className="mt-2 text-2xl font-black">{formatMoney(d.revenue,d.currency)}</div></div><div className="rounded-2xl border border-border bg-card p-5"><div className="text-xs text-muted-foreground">AOV</div><div className="mt-2 text-2xl font-black">{formatMoney(d.avgOrderValue,d.currency)}</div></div><div className="rounded-2xl border border-border bg-card p-5"><div className="text-xs text-muted-foreground">Orders</div><div className="mt-2 text-2xl font-black">{d.totalOrders.toLocaleString('fa-IR')}</div></div><div className="rounded-2xl border border-border bg-card p-5"><div className="text-xs text-muted-foreground">Low Stock</div><div className="mt-2 text-2xl font-black">{d.lowStockCount.toLocaleString('fa-IR')}</div></div></div><div className="grid gap-4 lg:grid-cols-3"><section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-bold">Top Products</h2><div className="mt-4 space-y-3">{d.topProducts.map(x=><div key={x.name} className="flex justify-between gap-3 text-sm"><span className="truncate">{x.name}</span><b>{formatMoney(x.revenue,d.currency)}</b></div>)}</div></section><section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-bold">Top Categories</h2><div className="mt-4 space-y-3">{d.topCategories.map(x=><div key={x.name} className="flex justify-between gap-3 text-sm"><span className="truncate">{x.name}</span><b>{formatMoney(x.revenue,d.currency)}</b></div>)}</div></section><section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-bold">Top Sellers</h2><div className="mt-4 space-y-3">{d.topSellers.map(x=><div key={x.name} className="flex justify-between gap-3 text-sm"><span className="truncate">{x.name}</span><b>{formatMoney(x.revenue,d.currency)}</b></div>)}</div></section></div></div>}
