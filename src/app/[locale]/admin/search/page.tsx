'use client';
import {useEffect,useState} from 'react';
export default function SearchManagementPage(){const [rows,setRows]=useState<any[]>([]);const [q,setQ]=useState('');async function load(){const r=await fetch(`/api/admin/search?q=${encodeURIComponent(q)}`);const j=await r.json();setRows(j.data??[])}useEffect(()=>{void load()},[]);return <div className="space-y-6"><header><h1 className="text-2xl font-black">Search Management</h1><p className="mt-1 text-sm text-muted-foreground">پرتکرارترین جستجوها و جستجوهای بدون نتیجه از داده واقعی Search API.</p></header><div className="flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void load()}} placeholder="جستجو در عبارت‌ها" className="w-full max-w-md rounded-xl border border-input bg-background px-3 py-2.5 text-sm"/><button onClick={()=>void load()} className="rounded-xl bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground">Search</button></div><div className="overflow-x-auto rounded-2xl border border-border bg-card"><table className="w-full text-sm"><thead className="bg-muted/40"><tr><th className="px-3 py-3 text-start">Query</th><th className="px-3 py-3 text-start">تعداد جستجو</th><th className="px-3 py-3 text-start">نتیجه</th><th className="px-3 py-3 text-start">آخرین بار</th></tr></thead><tbody className="divide-y divide-border">{rows.map(r=><tr key={r.id}><td className="px-3 py-3 font-semibold">{r.query}</td><td className="px-3 py-3">{String(r.searchCount)}</td><td className="px-3 py-3">{String(r.resultCount)}</td><td className="px-3 py-3 text-xs text-muted-foreground">{new Date(String(r.lastSearchedAt)).toLocaleString('fa-IR')}</td></tr>)}</tbody></table>{rows.length===0&&<div className="p-12 text-center text-sm text-muted-foreground">هنوز داده جستجو ثبت نشده است.</div>}</div></div>}
