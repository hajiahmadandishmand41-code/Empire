'use client';
import {useEffect,useState} from 'react';
export default function NotificationsPage(){const [rows,setRows]=useState<any[]>([]);const [form,setForm]=useState({audience:'all',title:'',body:'',href:'',kind:'system',isActive:true});async function load(){const r=await fetch('/api/admin/notifications');const j=await r.json();setRows(j.data??[])}useEffect(()=>{void load()},[]);async function save(){const r=await fetch('/api/admin/notifications',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});if(r.ok){setForm({...form,title:'',body:'',href:''});await load();}}return <div className="space-y-6"><header><h1 className="text-2xl font-black">Notification Center</h1><p className="mt-1 text-sm text-muted-foreground">اعلان‌های قابل مدیریت برای کاربران و فروشندگان.</p></header><section className="rounded-2xl border border-border bg-card p-5"><div className="grid gap-3 sm:grid-cols-2"><select value={form.audience} onChange={e=>setForm({...form,audience:e.target.value})} className="rounded-xl border border-input bg-background px-3 py-2.5"><option value="all">همه</option><option value="users">کاربران</option><option value="sellers">فروشندگان</option></select><input value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="عنوان" className="rounded-xl border border-input bg-background px-3 py-2.5"/><input value={form.href} onChange={e=>setForm({...form,href:e.target.value})} placeholder="لینک اختیاری" className="rounded-xl border border-input bg-background px-3 py-2.5"/><textarea value={form.body} onChange={e=>setForm({...form,body:e.target.value})} placeholder="متن اعلان" className="min-h-28 rounded-xl border border-input bg-background px-3 py-2.5 sm:col-span-2"/></div><button onClick={()=>void save()} className="mt-3 rounded-xl bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground">ایجاد اعلان</button></section><section className="space-y-2">{rows.map(r=><article key={r.id} className="rounded-2xl border border-border bg-card p-4"><div className="flex justify-between gap-3"><div><div className="font-semibold">{r.title}</div><div className="mt-1 text-sm text-muted-foreground">{r.body}</div></div><span className="text-xs text-muted-foreground">{r.audience}</span></div></article>)}</section></div>}
