'use client';
import * as React from 'react';
import Link from 'next/link';
export default function SellerOrdersPage(){const[items,setItems]=React.useState<any[]>([]);const[summary,setSummary]=React.useState<any>(null);const[loading,setLoading]=React.useState(true);React.useEffect(()=>{(async()=>{try{const r=await fetch('/api/seller/orders?pageSize=50',{credentials:'include',cache:'no-store'});const b=await r.json();if(r.ok&&b.ok){setItems(b.data?.items||[]);setSummary({total:b.data?.total??0})}}finally{setLoading(false)}})()},[]);return <div className="mx-auto max-w-7xl space-y-5" dir="rtl"><div className="flex items-end justify-between"><div><h1 className="text-2xl font-black">سفارش‌ها</h1><p className="mt-1 text-sm text-muted-foreground">سفارش‌های مرتبط با محصولات فروشگاه شما.</p></div>{summary&&<span className="rounded-full bg-muted px-3 py-1 text-xs font-bold">{Number(summary.total).toLocaleString('fa-IR')} سفارش</span>}</div><div className="overflow-hidden rounded-3xl border bg-card shadow-sm">{loading?<div className="p-10 text-center text-sm text-muted-foreground">در حال بارگذاری…</div>:items.length===0?<div className="p-10 text-center text-sm text-muted-foreground">هنوز سفارشی وجود ندارد.</div>:<div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b bg-muted/40 text-xs text-muted-foreground"><th className="p-4 text-right">شماره</th><th className="p-4 text-right">اقلام</th><th className="p-4 text-right">مبلغ</th><th className="p-4 text-right">پرداخت</th><th className="p-4 text-right">وضعیت</th><th className="p-4"></th></tr></thead><tbody>{items.map((o:any)=><tr key={o.id} className="border-b last:border-0"><td className="p-4 font-mono text-xs">#{o.reference}</td><td className="p-4">{Number(o.itemCount).toLocaleString('fa-IR')}</td><td className="p-4 font-bold">{Number(o.total).toLocaleString('fa-IR')} {o.currency}</td><td className="p-4 text-xs">{o.paymentStatus}</td><td className="p-4"><span className="rounded-full bg-muted px-2 py-1 text-[11px] font-bold">{o.status}</span></td><td className="p-4"><Link href={`./orders/${o.id}`} className="text-xs font-bold text-primary">جزئیات</Link></td></tr>)}</tbody></table></div>}</div></div>}
