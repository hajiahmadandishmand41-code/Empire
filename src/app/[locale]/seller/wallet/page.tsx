import { requireSeller } from '@/lib/auth/roles';
import { getWalletSummary, listWalletTransactions, listSellerPayouts } from '@/features/seller/lib/wallet-queries';
import { PayoutRequestForm } from '@/features/seller/components/payout-request-form';
import { formatMoney } from '@/features/admin/lib/format';
export const dynamic='force-dynamic';
export default async function WalletPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;const u=await requireSeller({locale});const [summary,tx,payouts]=await Promise.all([getWalletSummary(u.id),listWalletTransactions(u.id,20),listSellerPayouts(u.id,20)]);return <div className="mx-auto max-w-7xl space-y-5" dir="rtl"><div><h1 className="text-2xl font-black">کیف پول و تسویه</h1><p className="mt-1 text-sm text-muted-foreground">موجودی، درآمد، تراکنش‌ها و درخواست برداشت.</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div className="rounded-3xl border bg-card p-5 shadow-sm"><p className="text-xs text-muted-foreground">موجودی قابل برداشت</p><p className="mt-2 text-2xl font-black">{formatMoney(summary.balance,summary.currency)}</p></div><div className="rounded-3xl border bg-card p-5 shadow-sm"><p className="text-xs text-muted-foreground">کل درآمد</p><p className="mt-2 text-2xl font-black">{formatMoney(summary.totalEarned,summary.currency)}</p></div><div className="rounded-3xl border bg-card p-5 shadow-sm"><p className="text-xs text-muted-foreground">کل پرداخت‌شده</p><p className="mt-2 text-2xl font-black">{formatMoney(summary.totalPaidOut,summary.currency)}</p></div><div className="rounded-3xl border bg-card p-5 shadow-sm"><p className="text-xs text-muted-foreground">در انتظار تسویه</p><p className="mt-2 text-2xl font-black">{formatMoney(summary.pendingPayoutAmount,summary.currency)}</p></div></div><div className="grid gap-5 lg:grid-cols-[1.3fr_1fr]"><section className="rounded-3xl border bg-card p-5 shadow-sm"><h2 className="text-base font-black">آخرین تراکنش‌ها</h2><div className="mt-4 space-y-2">{tx.length===0?<p className="py-8 text-sm text-muted-foreground">تراکنشی وجود ندارد.</p>:tx.map(t=><div key={t.id} className="flex items-center justify-between rounded-2xl border p-3"><div><div className="text-sm font-bold">{t.description||t.type}</div><div className="text-[11px] text-muted-foreground">{new Date(t.createdAt).toLocaleDateString('fa-IR')}</div></div><div className={`font-black ${Number(t.amount)>=0?'text-emerald-600':'text-red-600'}`}>{Number(t.amount).toLocaleString('fa-IR')} {t.currency}</div></div>)}</div></section><div className="space-y-5"><PayoutRequestForm balance={summary.balance} currency={summary.currency}/><section className="rounded-3xl border bg-card p-5 shadow-sm"><h2 className="text-base font-black">درخواست‌های برداشت</h2><div className="mt-3 space-y-2">{payouts.length===0?<p className="py-4 text-sm text-muted-foreground">درخواستی ثبت نشده است.</p>:payouts.map(p=><div key={p.id} className="flex items-center justify-between rounded-2xl border p-3 text-sm"><span>{p.reference}</span><span className="font-bold">{Number(p.amount).toLocaleString('fa-IR')} {p.currency} · {p.status}</span></div>)}</div></section></div></div></div>}
