import Link from 'next/link';
import { listSellerCategories } from '@/features/seller/lib/products';
export const dynamic='force-dynamic';
export default async function CategoriesPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;const result=await listSellerCategories();return <div className="mx-auto max-w-7xl space-y-5" dir="rtl"><div><h1 className="text-2xl font-black">دسته‌بندی‌ها</h1><p className="mt-1 text-sm text-muted-foreground">دسته‌بندی‌های اصلی سایت و تعداد محصولات داخل هرکدام</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{result.items.map(c=><div key={c.id} className="rounded-3xl border bg-card p-5 shadow-sm"><div className="flex items-center justify-between"><div><div className="font-black">{c.name}</div><div className="mt-1 text-xs text-muted-foreground">/{c.slug}</div></div><span className="rounded-full bg-primary/10 px-3 py-1 text-xs font-bold text-primary">{c.productCount.toLocaleString('fa-IR')} محصول</span></div><Link href={`/${locale}/seller/products?q=${encodeURIComponent(c.name)}`} className="mt-4 inline-block text-xs font-bold text-primary">مشاهده محصولات ←</Link></div>)}</div></div>}
