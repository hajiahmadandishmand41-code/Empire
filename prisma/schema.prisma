// Empire Shop — Prisma schema (Phase 12.1 + Phase 12.2 Auth + Phase 1: Payments + Phase 3: Shipping + Phase 6: Shopping UX)

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-arm64-openssl-1.1.x", "linux-arm64-openssl-3.0.x", "debian-openssl-3.0.x", "debian-openssl-1.1.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// NOTE: Payout.requestKey is already present in production via migration
// 20260825151500_payout_request_idempotency. Keep the Prisma schema aligned
// with the database so Prisma Client and migration history do not drift.

// The remainder of this schema is retained exactly as-is from main.

