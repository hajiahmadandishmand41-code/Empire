-- Baseline for the production Supabase database.
-- The Empire schema was provisioned in Supabase before this Prisma migration
-- baseline was committed. This migration is intentionally non-destructive and
-- exists so `prisma migrate deploy` has a tracked baseline in production.
SELECT 1;
